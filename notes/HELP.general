

       Powershell , bash
  מעטפת פקודות

**WSL = Windows Subsystem for Linux
תספת של וינדוס שמריצה סביבת לינוקס אמיתית בתוך וינדוס
   נותן טרמינל לינוקסי (כמו אובונטו) עם הכלים המוכרים:
      ssh, scp, bash, grep, apt ...
  wsl2 - משתמש בגרסה של לינוקס מאחרי הקלעים
  פותחים : חפש "אובנטו" בתפריט ההתחלה או הרץ :
  wsl -l -v   # רשימת הפצות מותקנות

 ** Bash (WSL at linux) - Classic commands (awk, grep, cat, ls) and script file (.sh)
    script file (.ps1) variable like ($name)
 ** PowerShell : object world commend styl (get-childitem, select-string)
    script file (.ps1)
**  both know run file ( ssh scp)
           Example:
      # Bash
      ls -la
      grep "hello" file.txt
      export PORT=8081

      # PowerShell
      Get-ChildItem -Force
      Select-String "hello" file.txt
      $Env:PORT = 8081

           Spring Boot profile

 profile - מצב עבודה" עם קובץ הגדרות מתאים לכל סביבה
 קובץ כללי שחל על כולם - application.properties
 קובץ לפי פרופיל - application-<profile>.properties
    Example : application-dev.properties, application-prod.properties

         כשמפעילים עם
    --spring.profiles.active=prod

ספרינג יטען קודם את : application.properties
ואז יתן עדיפות לערכים שב - application-prod.properties
זה שימושי בגלל שלכל סביבה יש ערכים שונים :
  dev: server.port=8080 h2/DB , לוגים מפורטים
  MySQL: server.port=8081 prod פחות לוגים  - אמיתי

היכן מגדירים איזה פרופיל פעיל?
   pm2 start  (spring.profiles.active=prod) : פרמטר שורת פקודה
   SPRING_PROFILES_ACTIVE=prod : משתנה סביבה
   (פחות מומלץ לפרודקשן בגלל שזה מקבע בקוד)inside application.properties : spring.profiles.active=prod

איפה נמצא הקובץ?
   application-prod.properties מחוץ ל JAR בנתיבhome/moka/api
ולכן הוספנו:
   --spring.config.additional-location=/home/moka/api/
כדי לומר לספרינג חפש גם שם קבצי קונפיג..

איך יודעים איזה פרופיל נטען?
   בהדלקת האפליקציה בלוגים נראה שורה בסגנון :
   The following 1 profile is active: "prod"
   אפשר גם להדפיס בקוד, אבל לרוב הלוג מספק

                    pm2
              pm = process manager
      זה מנהל תהליכים לאפליקציות שרצות לאורך זמן ותפקידו:
     להריץ תהליכים
     (אם נופל להרים שוב)לפקח עליהם
     לשמור הגדרות
     להפעיל מחדש אחרי ריסטארט
     להציג לוגים


     ** יבוא של חבילות **
     כל מה שמתחיל ב־org.springframework = שייך ל־Spring.
     🔹 כל מה שמתחיל ב־java. = שייך לשפת ג׳אווה עצמה.
     🔹 כל מה שמתחיל ב־com.moka. = שייך לקוד שלי.

      ***תרשים הזרימה של המשחק***

      [1] המשתמש פותח את האתר
              ↓
      [2] הפרונטאנד שולח בקשה לשרת:
          GET /api/categories
              ↓
      [3] השרת מחזיר רשימת קטגוריות (היסטוריה, מתמטיקה, פילוסופיה)
              ↓
      [4] המשתמש בוחר קטגוריה
              ↓
      [5] הפרונטאנד שולח בקשה:
          GET /api/questions?categoryId=2
              ↓
      [6] השרת מחזיר את כל השאלות של הקטגוריה
              ↓
      [7] הפרונטאנד מציג את השאלה הראשונה
              ↓
      [8] המשתמש בוחר תשובה
              ↓
      [9] הפרונטאנד בודק אם התשובה נכונה
              ↓
      [10] אם נכונה → מציג את השאלה הבאה
           אם שגויה → מציג הודעת שגיאה
              ↓
      [11] בסוף השאלות → סיום המשחק + הצעת הרשמה לניקוד



   **** איפוס השרת במקרה של קוד שגוי***
מציאת IP - דרך האתר: https://whatismyipaddress.com
oot@srv787122:~# sudo fail2ban-client set sshd unbanip 89.139.80.165

התחברות לשרת דרך command prompt ****
>ssh root@69.62.109.118
התחברות לבסיס נתונים
 mysql -u moshe -p -h 127.0.0.1 -P 3306 triviaDB

